# 物联网案例: 车联网数据处理系统

> **所属阶段**: Knowledge/10-case-studies/iot | **前置依赖**: [../../02-design-patterns/pattern-event-time-processing.md](../../02-design-patterns/pattern-event-time-processing.md) | **形式化等级**: L4

---

> **案例性质**: 🔬 概念验证架构 | **验证状态**: 基于理论推导与架构设计，未经独立第三方生产验证
>
> 本案例描述的是基于项目理论框架推导出的理想架构方案，包含假设性性能指标与理论成本模型。
> 实际生产部署可能因环境差异、数据规模、团队能力等因素产生显著不同结果。
> 建议将其作为架构设计参考而非直接复制粘贴的生产蓝图。
>
## 目录

- [物联网案例: 车联网数据处理系统](#物联网案例-车联网数据处理系统)
  - [目录](#目录)
  - [1. 概念定义 (Definitions)](#1-概念定义-definitions)
    - [1.1 车联网系统定义](#11-车联网系统定义)
    - [1.2 车辆事件类型](#12-车辆事件类型)
  - [2. 实例验证 (Examples)](#2-实例验证-examples)
    - [2.1 案例背景](#21-案例背景)
    - [2.2 驾驶行为评分](#22-驾驶行为评分)
    - [2.3 性能指标](#23-性能指标)


## 1. 概念定义 (Definitions)

### 1.1 车联网系统定义

**Def-K-10-09-01** (车联网数据处理系统): 车联网系统是一个六元组 $\mathcal{V} = (C, S, D, N, A, \tau)$：

- $C$：车辆集合
- $S$：传感器集合（GPS、速度、引擎、电池等）
- $D$：数据流，$D = \{d | d = (t, v, s, location)\}$
- $N$：网络类型（5G/4G）
- $A$：分析应用（导航、保险、维护）
- $\tau$：延迟要求（导航<100ms，诊断<1s）

### 1.2 车辆事件类型

| 事件类型 | 频率 | 数据量 | 用途 |
|---------|------|-------|------|
| GPS位置 | 1Hz | 低 | 导航、轨迹 |
| 驾驶行为 | 10Hz | 中 | 保险评分 |
| 车辆状态 | 1Hz | 中 | 远程诊断 |
| 告警事件 | 触发 | 低 | 紧急响应 |
| 视频流 | 30fps | 高 | ADAS |

---

## 2. 实例验证 (Examples)

### 2.1 案例背景

**企业**: 某新能源汽车厂商

| 指标 | 数值 |
|-----|------|
| 联网车辆 | 200万辆 |
| 日均数据量 | 2PB |
| 数据上传频率 | 1-10Hz |
| 服务场景 | 导航/保险/维护/OTA |

### 2.2 驾驶行为评分

```java
/**
 * 驾驶行为实时评分
 */

import org.apache.flink.streaming.api.environment.StreamExecutionEnvironment;
import org.apache.flink.streaming.api.datastream.DataStream;
import org.apache.flink.streaming.api.windowing.time.Time;

public class DrivingBehaviorScoring {

    public static void main(String[] args) throws Exception {
        StreamExecutionEnvironment env = StreamExecutionEnvironment.getExecutionEnvironment();

        DataStream<VehicleEvent> events = env
            .fromSource(createKafkaSource(),
                WatermarkStrategy.<VehicleEvent>forBoundedOutOfOrderness(Duration.ofSeconds(5))
                    .withIdleness(Duration.ofMinutes(1)),
                "Vehicle Events")
            .setParallelism(128);

        // 驾驶行为分析(5分钟窗口)
        DataStream<DrivingScore> scores = events
            .keyBy(VehicleEvent::getVehicleId)
            .window(TumblingEventTimeWindows.of(Time.minutes(5)))
            .process(new DrivingBehaviorAnalyzer())
            .name("Driving Score")
            .setParallelism(256);

        // 写入保险平台
        scores.addSink(new InsurancePlatformSink());

        env.execute("Driving Behavior Scoring");
    }
}

/**
 * 驾驶行为分析
 */
class DrivingBehaviorAnalyzer extends ProcessWindowFunction<VehicleEvent, DrivingScore, String, TimeWindow> {

    @Override
    public void process(String vehicleId, Context ctx, Iterable<VehicleEvent> events,
                       Collector<DrivingScore> out) {
        int harshBraking = 0;
        int harshAcceleration = 0;
        int sharpTurn = 0;
        int speeding = 0;
        double totalDistance = 0;
        long totalDrivingTime = 0;

        VehicleEvent lastEvent = null;

        for (VehicleEvent event : events) {
            // 急刹车检测
            if (event.getAcceleration() < -3.0) {  // m/s²
                harshBraking++;
            }

            // 急加速检测
            if (event.getAcceleration() > 3.0) {
                harshAcceleration++;
            }

            // 急转弯检测
            if (Math.abs(event.getGyroZ()) > 0.5) {  // rad/s
                sharpTurn++;
            }

            // 超速检测
            if (event.getSpeed() > event.getSpeedLimit() * 1.2) {
                speeding++;
            }

            // 计算距离
            if (lastEvent != null) {
                totalDistance += GeoUtils.distance(lastEvent.getLocation(), event.getLocation());
            }

            lastEvent = event;
        }

        // 计算评分(0-100)
        double score = 100;
        score -= harshBraking * 2;
        score -= harshAcceleration * 2;
        score -= sharpTurn * 1.5;
        score -= speeding * 3;
        score = Math.max(0, score);

        out.collect(new DrivingScore(
            vehicleId,
            ctx.window().getStart(),
            score,
            harshBraking,
            harshAcceleration,
            sharpTurn,
            speeding,
            totalDistance
        ));
    }
}
```

### 2.3 性能指标
>
> 🔮 **估算数据** | 依据: 设计目标值，实际达成可能因环境而异


| 指标 | 目标值 | 实际值 |
|------|-------|-------|
| 数据处理延迟 | < 5s | 2s |
| 日处理事件 | 200亿 | 250亿 |
| 位置精度 | 10m | 5m |
| 系统可用性 | 99.99% | 99.995% |

---

*文档版本: v1.0 | 最后更新: 2026-04-04*
